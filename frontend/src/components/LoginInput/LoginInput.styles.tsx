import { Box, InputBase, styled } from '@mui/material';

export const CustomTextField = styled(InputBase)({
  marginLeft: 3,
  marginRight: 3
});

export const CustomTextFieldBody = styled(Box)({
  background: 'rgb(0,0,0,.1)',
  padding: 4,
  borderRadius: 4
});
