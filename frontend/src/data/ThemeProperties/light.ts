import type { PaletteOptions } from '@mui/material';

const light: PaletteOptions = {
  LoginInputBG: 'rgba(0, 0, 0, 0.1)',
  LoginSubtitleColor: 'rgba(0, 0, 0, 1)'
} as PaletteOptions;

export default light;
