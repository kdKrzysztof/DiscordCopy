import type { PaletteOptions } from '@mui/material';

const dark: PaletteOptions = {
  LoginInputBG: 'rgba(0, 0, 0, 0.5)',
  LoginSubtitleColor: 'rgba(255, 255, 255, 0.7)'
} as PaletteOptions;

export default dark;
